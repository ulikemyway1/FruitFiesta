enum Hash {
  LOGIN = "#login",
  REGISTRATION = "#registration",
  MAIN = "#main",
  CATALOG = "#catalog",
  // CATEGORY = "#category",
  PRODUCT = "#product",
  BASKET = "#basket",
  ABOUT = "#about",
  NOT_FOUND = "#not-found",
  PROFILE = "#profile",
}

export default Hash;

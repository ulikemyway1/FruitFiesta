enum Hash {
  LOGIN = "#login",
  REGISTRATION = "#registration",
  MAIN = "#main",
  CATALOG = "#catalog",
  DETAIL = "#detail",
  BASKET = "#basket",
  ABOUT = "#about",
  NOT_FOUND = "#not-found",
  PROFILE = "#profile",
}

export default Hash;

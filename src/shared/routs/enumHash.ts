enum Hash {
  LOGIN = "#login",
  REGISTRATION = "#registration",
  MAIN = "#main",
  CATALOG = "#catalog",
  DETAIL = "#detail",
  USER = "#user",
  BASKET = "#basket",
  ABOUT = "#about",
  NOT_FOUND = "#not-found",
  PROFILE = "#profile",
  // EMPTY = "",
}

export default Hash;
